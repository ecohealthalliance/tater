template(name="groupDocuments")
  .group-documents.documents
    .view-header.secondary-bg
      .container
        h1.inline Documents
        if showNewDocumentLink
          a.new-document-link.btn.header-btn.btn-primary(href="{{path route='newDocument' params=newDocumentParams}}")
            span Add Document
            i.fa.fa-file-text-o
        p.group-name
          span Group:
          | #{group.name}
          if group.codeAccessible
            | (Code Accessible)
    .container
      if documents
        ul.document-list.list.list-unstyled
          each documents
            li.document
                a.list-link(href="{{path route='documentDetail' params=this}}")
                  span.document-title= title
                button.btn.btn-primary.delete-document-button(data-document-id=_id) Delete Document
      else
        p No documents have been uploaded.
