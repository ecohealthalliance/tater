template(name="admin")
  if currentUser
    if isAdmin
      .groups-container
        +groups
      .users-container
        +users
  else
    .container
      .row
        .col-sm-12
          p Please log in or sign up to continue.
