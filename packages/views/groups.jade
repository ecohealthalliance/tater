template(name="groups")
  .container-list
    if Template.subscriptionsReady
      if groups
        .group-list.list-table.groups-table.no-filter.table-wrap
          +reactiveTable collection=groups settings=settings filters=filters
    else
      .loading.center-v
